import math as m
import random as rand

#actions, what each character can do
def test():
	print("the test works")
	
#have to create two characters
#level them up, print their stats
#then need some kind of action that deals damage to them, testing how much is done with each hit
#that 'attack action' must be used by both parties, then each turn recorded